// const let = "pallavi";
console.log("let", let)

// await keyword is only reserved within the body of an async function
let await = "laxman"
console.log("await", await)

// const els\u{65} = 1;
// `els\u{65}` encodes the same identifier as `else`
